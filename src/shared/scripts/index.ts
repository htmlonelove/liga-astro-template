import { mobileVhFix } from '@shared/helpers'

mobileVhFix()
document.addEventListener(
  'DOMContentLoaded',
  () => {
    // initModals()
    // let accordions = new Accordions()
    // accordions.init()
  },
  true
)
